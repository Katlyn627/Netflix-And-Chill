# Netflix and Chill - Environment Configuration

# TMDB API Key (The Movie Database)
# Get your free API key at: https://www.themoviedb.org/settings/api
# This is OPTIONAL but recommended for the seeder to generate realistic movie/TV data
TMDB_API_KEY=

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# Options: 'file' (default), 'mongodb', 'postgresql'
DB_TYPE=file

# MongoDB Configuration (only needed if DB_TYPE=mongodb)
# MONGODB_URI=mongodb://localhost:27017/netflix-and-chill

# PostgreSQL Configuration (only needed if DB_TYPE=postgresql)
# PG_HOST=localhost
# PG_PORT=5432
# PG_DATABASE=netflix_and_chill
# PG_USER=postgres
# PG_PASSWORD=
